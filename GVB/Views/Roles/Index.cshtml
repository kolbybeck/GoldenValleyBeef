@model IEnumerable<GVB.Models.Roles>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create", null, new { @class = "btn btn-info btn-large" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RoleDescr)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RoleDescr)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.RoleID }, new { @class = "btn btn-info btn-large" })
                @Html.ActionLink("Delete", "Delete", new { id = item.RoleID }, new { @class = "btn btn-info btn-large" })
            </td>
        </tr>
    }

</table>

